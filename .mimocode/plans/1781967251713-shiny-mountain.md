# Plan: Delete Auth & Multi-Language Code

## Files to DELETE entirely (8 files)

### Auth
1. `src/app/auth/page.jsx` — entire login/signup page
2. `src/lib/firebase.js` — Firebase init, auth, googleProvider

### Multi-Language
3. `src/locales/en.json` — English translations
4. `src/locales/hi.json` — Hindi translations
5. `src/locales/hinglish.json` — Hinglish translations
6. `src/hooks/useTranslation.js` — translation hook
7. `src/lib/utils/translateHelper.js` — Google Translate API helper

### Also delete
8. `src/lib/dbSync.js` — Supabase cloud sync (tied to auth users)

---

## Files to EDIT (strip auth + i18n code) — 10 files

### 1. `src/lib/store/useAppStore.js`
- Remove `import { fetchUserProfile, syncUserProfile } from '@/lib/dbSync'`
- Remove from `initialState`: `hasCompletedOnboarding`, `user`, `isAuthenticated`, `isEmailVerified`, `isPhoneVerified`
- Remove actions: `loginUser`, `logout`, `setUser`, `setIsAuthenticated`, `setIsEmailVerified`, `setIsPhoneVerified`, `setHasCompletedOnboarding`
- Remove `language` references if any
- Remove the auto-sync subscriber at the bottom (`useAppStore.subscribe`)

### 2. `src/app/page.jsx`
- Remove `isAuthenticated` from useAppStore destructuring
- Remove auth redirect logic (the CTA button `router.push(isAuthenticated ? '/home' : '/auth')` → just `/home`)
- Remove auth guard redirects if any

### 3. `src/app/dashboard/page.jsx`
- Remove `import { useTranslation } from '@/hooks/useTranslation'`
- Remove `import { translateObject, translateText } from '@/lib/utils/translateHelper'`
- Remove `const { t, locale } = useTranslation()`
- Remove all `translatedModules`, `translatedQuotes`, `translatedQuickTools`, `translatedGreeting` state + useEffect translation logic
- Replace `translatedModules` with `modules`, `translatedQuotes` with `QUOTES`, etc.
- Remove `isAuthenticated` check and auth redirect
- Replace `t('auth.title_login')` calls with hardcoded strings

### 4. `src/app/tools/page.jsx`
- Remove `translatedTools`, `translatedToolsTitle`, `translatedToolsSub` vars (just use hardcoded values)
- Remove `isAuthenticated` check (keep tools always accessible)

### 5. `src/app/profile/page.jsx`
- Remove auth guards (`if (!isAuthenticated) router.replace('/')`)
- Remove `isAuthenticated` from store destructuring

### 6. `src/app/home/layout.jsx`
- Remove auth guards (`if (!hydrated || !isAuthenticated)`)
- Remove `isAuthenticated` from store destructuring
- Remove `Globe` icon import (used for language selector)

### 7. `src/app/home/[tab]/page.jsx`
- Remove language selector UI (Globe button + dropdown with `showLangMenu`, `activeLang`, `handleLangSelect`)
- Remove `translatedModules`, `translatedQuotes`, `translatedGreeting` vars
- Remove auth guards (`if (!isAuthenticated)`)
- Replace translation-dependent rendering with direct values

### 8. `src/app/home/dashboard/page.jsx`
- Remove any translation-related state/logic if present
- Replace `translatedModules`/`translatedQuotes`/`translatedGreeting` with direct values

### 9. `src/app/dashboard/module/[id]/page.jsx`
- Remove `isAuthenticated` check and auth redirect

### 10. `src/app/strategy/[slug]/page.jsx`
- Remove `isAuthenticated` check and auth redirect

---

## What STAYS (no changes needed)
- `src/app/home/page.jsx` — just redirects to `/home/dashboard`, no auth code
- `src/components/2d/navbar.jsx` — no auth imports (uses store but no Firebase)
- `src/components/shared/*` — no auth/i18n imports
- `src/components/strategies/*` — no auth/i18n imports
- `src/app/api/*` — API routes stay
- `src/lib/data/*` — data files stay
- `src/lib/constants.js` — stays
- `src/lib/utils.js` — stays
- `src/lib/supabase.js` — keep (used by chatbot potentially)
- `src/lib/supabase-chatbot.js` — stays
- `src/middleware.js` — stays (rate limiting, not auth)

---

## Verification
1. Run `npm run dev` and verify app loads without errors
2. Check `/` landing page renders
3. Check `/home/dashboard` renders without auth redirect
4. Check `/tools` renders without auth guard
5. Check `/profile` renders
6. Grep for any remaining `firebase`, `useTranslation`, `translateText`, `isAuthenticated` imports to confirm cleanup
7. Run `npm run lint` to check for unused imports/errors
